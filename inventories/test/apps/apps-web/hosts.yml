---
# Test Apps Web Inventory

all:
  children:
    apps_web:
      children:
        web_servers:
          hosts:
            web01.test.apps.local:
              ansible_host: 10.102.1.10
            web02.test.apps.local:
              ansible_host: 10.102.1.11
        static_servers:
          hosts:
            static01.test.apps.local:
              ansible_host: 10.102.1.20

      vars:
        environment: test
        environment_tier: apps
        compartment: apps-web
        ansible_user: ansible
        ansible_become: true
