---
# Test Apps Data Inventory

all:
  children:
    apps_data:
      children:
        postgresql_servers:
          hosts:
            pg01.test.apps.local:
              ansible_host: 10.102.3.10
        redis_servers:
          hosts:
            redis01.test.apps.local:
              ansible_host: 10.102.3.20
        elasticsearch_servers:
          hosts:
            es01.test.apps.local:
              ansible_host: 10.102.3.30

      vars:
        environment: test
        environment_tier: apps
        compartment: apps-data
        ansible_user: ansible
        ansible_become: true
