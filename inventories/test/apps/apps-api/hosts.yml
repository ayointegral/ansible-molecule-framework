---
# Test Apps API Inventory

all:
  children:
    apps_api:
      children:
        api_servers:
          hosts:
            api01.test.apps.local:
              ansible_host: 10.102.2.10
            api02.test.apps.local:
              ansible_host: 10.102.2.11
        queue_servers:
          hosts:
            rabbitmq01.test.apps.local:
              ansible_host: 10.102.2.20

      vars:
        environment: test
        environment_tier: apps
        compartment: apps-api
        ansible_user: ansible
        ansible_become: true
