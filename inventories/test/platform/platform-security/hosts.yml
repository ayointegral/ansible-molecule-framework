---
# Test Platform Security Inventory

all:
  children:
    platform_security:
      children:
        firewalls:
          hosts:
            fw01.test.platform.local:
              ansible_host: 10.101.3.10
        waf_servers:
          hosts:
            waf01.test.platform.local:
              ansible_host: 10.101.3.20
        siem_servers:
          hosts:
            siem01.test.platform.local:
              ansible_host: 10.101.3.30
        vault_servers:
          hosts:
            vault01.test.platform.local:
              ansible_host: 10.101.3.40

      vars:
        environment: test
        environment_tier: platform
        compartment: platform-security
        ansible_user: ansible
        ansible_become: true
