---
# Test Platform Network Inventory

all:
  children:
    platform_network:
      children:
        load_balancers:
          hosts:
            lb01.test.platform.local:
              ansible_host: 10.101.2.10
        vpn_servers:
          hosts:
            vpn01.test.platform.local:
              ansible_host: 10.101.2.20
        proxy_servers:
          hosts:
            proxy01.test.platform.local:
              ansible_host: 10.101.2.30

      vars:
        environment: test
        environment_tier: platform
        compartment: platform-network
        ansible_user: ansible
        ansible_become: true
