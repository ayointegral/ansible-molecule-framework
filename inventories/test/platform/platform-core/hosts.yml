---
# Test Platform Core Inventory
# Core infrastructure services: DNS, DHCP, Time, Authentication

all:
  children:
    platform_core:
      children:
        dns_servers:
          hosts:
            dns01.test.platform.local:
              ansible_host: 10.101.1.10
        dhcp_servers:
          hosts:
            dhcp01.test.platform.local:
              ansible_host: 10.101.1.20
        ntp_servers:
          hosts:
            ntp01.test.platform.local:
              ansible_host: 10.101.1.30
        auth_servers:
          hosts:
            ldap01.test.platform.local:
              ansible_host: 10.101.1.40

      vars:
        environment: test
        environment_tier: platform
        compartment: platform-core
        ansible_user: ansible
        ansible_become: true
