---
# Test Platform Storage Inventory

all:
  children:
    platform_storage:
      children:
        nfs_servers:
          hosts:
            nfs01.test.platform.local:
              ansible_host: 10.101.4.10
        s3_servers:
          hosts:
            minio01.test.platform.local:
              ansible_host: 10.101.4.20
        backup_servers:
          hosts:
            backup01.test.platform.local:
              ansible_host: 10.101.4.30

      vars:
        environment: test
        environment_tier: platform
        compartment: platform-storage
        ansible_user: ansible
        ansible_become: true
